# RatLab — Bazel Quick Reference

(Auto-generated by an LLM, human-modified by:
- VertaireStudio / vertairestudio@protonmail.com)

## Building

| Command | Description |
|---------|-------------|
| `bazel build //...` | Build all targets (default C++20) |
| `bazel build cpp17 //...` | Build targeting C++17 |
| `bazel build cpp23 //...` | Build targeting C++23 |
| `bazel build cpp26 //...` | Build targeting C++26 |
| `bazel build //:ratlab` | Build only the core library |
| `bazel build --keep_going //...` | Build all, report errors but don't stop on first failure |

## Testing

| Command | Description |
|---------|-------------|
| `bazel build //Tests:math_funcs_test` | Compile the test source file |
| `bazel-bin/Tests/math_funcs_test` | After compiling, run the test | 
| `bazel test //...` | Run tests without a visual header |

## Benchmarks

| Command | Description |
|---------|-------------|
| `bazel build //Benchmarks:math_funcs_bench` | Compile the benchmark source file |
| `bazel-bin/Benchmarks/math_funcs_bench` | After compiling, run the benchmark | 

## Query & Inspection

| Command | Description |
|---------|-------------|
| `bazel query //...` | List all targets in the workspace |
| `bazel query 'kind("cc_.*", //...)'` | List only C++ targets |
| `bazel query 'deps(//:ratlab)'` | Show transitive dependencies of core |
| `bazel query 'somepath(//Tests:..., //Core:core)'` | Find dependency path between targets |

## Cleaning

| Command | Description |
|---------|-------------|
| `bazel clean` | Remove build output symlinks and intermediate files |
| `bazel clean --expunge` | Full clean, removes entire output base |

## Useful Flags

| Flag | Description |
|------|-------------|
| `--config=ci` | Echo which rc options were applied |
| `-k` / `--keep_going` | Don't stop on first error |
| `-j N` | Limit parallel jobs to N |
| `--subcommands` | Print the shell commands Bazel runs |

## Notes

- Bzlmod (`MODULE.bazel`) is used for dependency management, not `WORKSPACE`.
- C++ standard is selected via `--config=cppXX`; default is **C++20**.
- `//Core:core` is header-only — no compiled `.o` artifacts.
- Test framework is custom (`tester.hpp`), not gtest.
